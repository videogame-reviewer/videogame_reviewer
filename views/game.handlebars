<section class="container">
  <h3 class="gallery-name">{{game.name}}</h3>
  {{!-- <h4>Opening Hour: {{format_time gallery.starting_date}}</h4>
  <h4>Closing Hour: {{format_time gallery.ending_date}}</h4> --}}
  <img src="/images/{{game.filename}}" alt="">
  <section class="flex-row justify-center">
    {{#each game.reviews as |reviews|}}
    {{> reviews-details}}
    {{/each}}
    <div class="gDescription">{{game.description}}</div>
    <div class="gGenre">Genre: {{game.genre}}</div>
  </section>
</section>

<section id="comments">
    {{#if loggedIn}}
  <section class="top">
    <div class="gButtons">
      <button onclick = "window.location = '{{game.purchaselink}}';"
 class="buy">Press here to buy!</button>
      <button class="makeReview">Leave a review.</button>
    </div>
  </section>

  <section class="re">
    <div class="leaveReview">
      <h5>What do ya think about it?</h5>
      <input type="text" name="ReviewText">
    </div>
  </section>
    {{else}}
      <a href="/login"></a>
    {{/if}}
</section>