<main id="full-game-main">
  <section class="full-game-description">
    <section class="container">
      <h3 class="game-name">{{game.name}}<br></h3>
      <img src="/images/{{game.filename}}" alt="{{game.[0].description}}">
        <div class="gButtons">
          <button onclick="window.location = '{{game.purchaselink}}', '_blank'"  class="buy">Press here to buy!</button>
        </div>
    </section>
    <section id="game-description">
          <div class="game-description">{{game.description}}<span id="genreHB"><br><hr>Genre: {{game.genre}}</span></div>
    </section>
  </section>

  <section id="comments">
    {{#if loggedIn}}
    <section class="top">
    </section>

    <section class="comments-section">
      <form class="leaveReview">
        <input type="text" name="ReviewText"
        placeholder="Please type your review here..." id="review-input" data-game-id={{game.id}}>
        <button class="makeReview" type="submit">Submit</button>
      </form>
      {{reviews-details}}
          {{#each game.reviews as |reviews|}}
      {{> reviews-details}}
      {{/each}}
    </section>
    {{else}}
    <a href="/login"></a>
    {{/if}}
  </section>
</main>
<script src="../js/review.js"></script>